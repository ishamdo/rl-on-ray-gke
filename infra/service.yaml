# Stable Service name shared by the HTTPRoute backend and any hub_router target.
apiVersion: v1
kind: Service
metadata:
  name: dolev-rl-controller
  namespace: ${NAMESPACE}
  labels:
    app: dolev-rl-controller
spec:
  selector:
    app: dolev-rl-controller
  ports:
    - name: http
      port: 80
      targetPort: 8000
